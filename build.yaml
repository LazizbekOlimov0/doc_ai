targets:
  $default:
    sources:
      - lib/**
      - i18n/**
      - strings*.i18n.json
    builders:
      slang_build_runner:
        options:
          base_locale: uz
          fallback_strategy: base_locale
